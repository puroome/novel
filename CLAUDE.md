# Novel Reading Quiz — 작업 안내

여러 소설의 어휘·배경지식·퀴즈를 제공하는 학습 앱이다. 구조와 시트 형식은 `README.md`에
정리되어 있으니 **먼저 그것을 읽는다.** 이 문서는 README에 없는 결정과 진행 상황을 남긴다.

## 지금 상태 (2026-08-25)

- 앱은 **다중 소설**을 지원한다. `wonder`(기존)와 `tiger`(신규)가 등록되어 있다.
- **로컬에서만 확인했다.** GitHub에는 아직 올리지 않았다. `npm --prefix novel-main start` →
  http://localhost:4173 (127.0.0.1이 아니라 `localhost`로 접속해야 Firebase 로그인이 된다).
- tiger는 **1~3장만** 들어 있다 (어휘 36, 배경지식 9, 퀴즈 18). 4~46장이 남았다.
- wonder 어휘 **1177행 전체의 논항 표기 정비와 내용 검증이 끝났다.** 여덟 파트 모두
  **시트 반영 대기** — `novel-data/wonder/normalized/paste/`에 붙여넣기용 사본이 있다.
- wonder **배경지식은 1~5장만 정비했다**(11행 → 17행, `wonder/bg-update/`). 6~123장이 남았고
  **bg가 0개인 챕터가 28개**다. 다음에 할 일은 이것이다.
- **계정·세션이 바뀌면 `novel-data/README.md` 맨 앞의 「다른 계정·세션에서 이어받을 때」를
  먼저 읽는다.** 현재 상태와 다음 할 일, 제약이 거기 정리되어 있다.
- `npm test`는 **45/46 통과**. 남은 1개(`앱에 Markdown 파싱 잔재가 남아 있지 않다`)는
  이 작업 이전부터 실패하던 것이다. 아래 "손대지 않은 문제" 참고.

## 다중 소설 구조에서 알아둘 점

- 소설 목록은 `novels` 시트가 원본이고, 동기화하면 `novel/content/novels`에 올라간다.
  앱은 **이 노드를 읽어 승인 여부를 판단한다.** 노드가 없으면 로그인 자체가 막히므로,
  `Code.gs`를 새로 배포한 뒤에는 반드시 `학습 자료 동기화 (전체)`를 한 번 돌린다.
- `novels` 시트의 `id`는 세 곳에서 같은 이름으로 쓰인다. **시트 이름 접두어**(`tiger_word`),
  **`AllowedUsers`의 열 이름**, **Firebase 경로**(`novel/content/tiger`).
- `AllowedUsers`의 소설별 열은 **빈칸이면 오류**다. 반드시 `yes` 또는 `no`.
- 소설별 권한 목록은 브라우저 `sessionStorage`에 담아 둔다. 시트를 고치고 다시 동기화해도
  **새로고침만으로는 바뀌지 않는다.** 탭을 새로 열거나 `sessionStorage.clear()`를 실행한다.

## 소설 원본과 자료 작업

수업용 사용은 **교육청 승인을 받았다.** 저작권 때문에 막을 것은 없다.

원본 epub, 추출한 본문, 생성·검증 스크립트는 `novel-data/`에 있고
**`.gitignore`로 제외되어 GitHub에는 올라가지 않는다.** 앱은 이 파일들을 런타임에 전혀
읽지 않는다 — 코드 어디에도 `epub`이나 `.pdf` 참조가 없다. GitHub Pages가 저장소 전체를
서빙하므로 올릴 이유도 없다.

**자료를 만들기 전에 `novel-data/README.md`를 읽는다.** 특히 두 절이 기준이다.

- **「어휘(word) 자료를 만드는 규칙」** — `meaning`의 ①은 반드시 그 행의 `sentence`에서
  쓰인 뜻이고 품사까지 같아야 한다. ② 이하는 고1~2 빈도순 두세 개. 사전 순서를 그대로
  옮기면 대개 ①이 어긋난다.
- **「작업 방식」** — 20행씩 끊어서 만들고 그때그때 본문과 대조한다. **형식 검사를
  통과했다고 뜻이 맞는 것이 아니다.** 스크립트는 표기와 구조만 본다.

**작업을 마칠 때마다 그 문서의 진행 표와 작업 기록을 갱신한다.** 사용자가 시키지 않아도
한다. 세션이 바뀌어도 같은 품질이 나오게 하는 유일한 장치다. 사용자가 방향을 바로잡아
주면 규칙 자체를 고치고 **왜 바뀌었는지 이유까지 남긴다.**

> `.gitignore`에서 `novel-data/`를 빼면 원서 전문이 그대로 올라간다. 지우지 않는다.

## 아직 하지 않은 것

- **소설별 접근 제어가 화면 단에서만 동작한다.** `firebase.rules.json`은 여전히
  `novel/content` 전체를 승인된 사용자에게 열어 준다. tiger만 허가된 학생도 마음먹으면
  wonder 자료를 받아볼 수 있다. 사용자가 "일단 화면에서만 숨김"을 택한 결과다.
  서버에서 막으려면 규칙을 `novel/content/$novelId` 단위로 쪼개고
  `accessByUid/$uid/novels/$novelId`를 검사하면 된다. **Apps Script는 이미 그 값을 올리고
  있으므로 규칙만 고치면 된다.**
- `manifest.json`의 이름이 아직 `Wonder Reading Journey`다. 설치된 앱 이름이 바뀌는 일이라
  사용자 확인을 받고 고친다.

## 손대지 않은 문제

`tests/firebase-integration.test.mjs`의 `앱에 Markdown 파싱 잔재가 남아 있지 않다`가 실패한다.
`js/library-loader.js`, `js/content-parser.js`, `scripts/generate-manifest.mjs`가 없어야 한다고
검사하는데 파일이 남아 있다. 그런데 `tests/library-loader.test.mjs`와
`tests/content-parser.test.mjs`는 **그 파일들을 import 한다.** 지우면 다른 테스트가 깨진다.
마이그레이션이 중간에 멈춘 상태이며, 이번 작업과 무관하므로 건드리지 않았다.

## CSS

`npm run build:css`가 **정상 동작한다**(네트워크 필요). `index.html`이나 `js/*.js`에서
클래스를 추가·삭제했다면 반드시 다시 빌드하고 `styles.css`도 함께 커밋한다.
빠뜨리면 `tests/styles-build.test.mjs`가 잡아 준다.

## 사용자 선호

- 대화는 한국어로 한다.
- **`assets/` 폴더에 임시 파일을 만들지 않는다.** 과거에 레이아웃 확인용 더미 이미지를
  만들었다가 지우면서 사용자가 넣어 둔 실제 파일을 삭제한 적이 있다. 검증이 필요하면
  스크래치패드에 사본을 만들어 쓴다.
- 코드를 고치기 전에 검토만 요청하는 경우가 있다. "검토만 해줘"라고 하면 코드를 건드리지 않는다.
