# Novel Reading Quiz — 작업 안내

여러 소설의 어휘·배경지식·퀴즈를 제공하는 학습 앱이다. 구조와 시트 형식은 `README.md`에
정리되어 있으니 **먼저 그것을 읽는다.** 이 문서는 README에 없는 결정과 진행 상황을 남긴다.

## 지금 상태 (2026-08-25)

- 앱은 **다중 소설**을 지원한다. `wonder`(기존)와 `tiger`(신규)가 등록되어 있다.
- **로컬에서만 확인했다.** GitHub에는 아직 올리지 않았다. `npm --prefix novel-main start` →
  http://localhost:4173 (127.0.0.1이 아니라 `localhost`로 접속해야 Firebase 로그인이 된다).
- tiger는 **어휘 948행 · 배경지식 78행 · 퀴즈 254문항으로 1~46장이 모두 끝났다** (2026-08-26).
  **46장을 네 파트로도 묶었다** — 원서에는 파트가 없어 내용을 보고 새로 지은 것이며 사용자와
  상의해 정했다(1~13 / 14~21 / 22~35 / 36~46). `part_title` 열만 바꾸는 `tiger/_set_parts.py`로
  처리하므로 경계를 다시 손보려면 그 파일만 고쳐 다시 돌린다. 자세한 근거는 `novel-data/README.md`의
  「tiger 파트 나누기」 절에 있다.
  셋 다 **시트 반영만 남았다** — `tiger/tiger_word_FIXED_ALL.tsv`, `tiger/bg-v3/tiger_bg_ALL.tsv`,
  `tiger/quiz-v1/tiger_quiz_ALL.tsv`. 모두 헤더가 없고 기존 행을 통째로 교체한다.
- **`part_title`은 `{id}_word` 시트에만 둔다** (2026-08-26). `{id}_bg`도 `{id}_quiz`처럼 그 열이
  없고, 같은 `chapter_no`의 word 행에서 물려받는다. 같은 값을 두 시트에 적어 두면 파트를 손볼 때
  어긋나기 때문이며 실제로 어긋나 있었다. **새 소설을 추가할 때 bg 시트에는 이 열을 만들지
  않는다.** 기존 `tiger_bg`·`wonder_bg` 시트의 A열은 지우면 되는데, **남겨 두어도 앱은 정상
  동작한다** — `Code.gs`가 헤더를 이름으로 찾아 필요한 열만 읽으므로 남은 열은 그냥 무시된다.
  로컬 데이터·도구·문서는 이미 다 바꿨다. 근거와 범위는 `novel-data/README.md`의
  「bg 시트에서 `part_title` 열을 뺌」 절에 있다.
- **`{id}_word` 시트의 `page` 열도 없앴다** (2026-08-26). 학생은 저마다 다른 출판사의 실물
  책으로 읽는데 자료가 참고한 것은 epub 한 판본뿐이라 쪽번호가 아무에게도 맞지 않는다.
  앱은 이 값을 쓴 적이 없다(`js/`·`index.html`에 참조 0건). **새 소설의 word 시트는 여덟 열로
  만든다.** 시트의 `page` 열(마지막 열)은 지우면 되고, 남겨 두어도 무시된다. 원문 추출의
  `[p.NNN]` 표시는 작업용이라 그대로 둔다. 근거는 `novel-data/README.md`의
  「page 열을 없앤 이유」 절에 있다.
- **TSV를 다시 쓸 때 `csv` 모듈을 쓰지 않는다.** 큰따옴표가 든 필드에 인용부호가 붙어
  탭으로 쪼개 읽는 도구(`check_part.py` 등)가 깨진다. 이번에 실제로 한 번 깨뜨렸다.
  줄바꿈이 섞인 파일도 있으니 줄마다 원래 끝을 보존해야 한다.
- **퀴즈는 `wonder_quiz` 653행을 실측해 관례를 맞췄다**(챕터당 5.5·영어 발문 31%·`explanation`
  합니다체). `quiz-v1/_build.py`가 **`evidence`가 그 챕터 원문에 정말 있는지 대조하고**
  정답 위치를 1~4에 고르게 편다. 원문 대조가 실제로 챕터를 착각한 문항 하나를 잡아냈다.
- **배경지식은 세 번 만들었고 세 번째(78행)가 최종본이다** — `novel-data/tiger/bg-v3/`.
  5장 묶음 9개 파일과 합본 `tiger_bg_ALL.tsv`가 있다(헤더 없음). `bg-update/`(387행)와
  `tiger_bg_FIXED_ALL.tsv`(178행)는 **폐기된 이전 판이니 쓰지 않는다.**
  387행본은 사용자가 `I make a wish on one of them.`·`call 911`·`Halmoni`·`Unya`를 짚으며
  "이건 배경지식이 아니다"라고 지적해 통째로 버렸다. **원인은 지침이었다** — wonder(미국
  아이의 미국 이야기)용 층 목록을 tiger(재미 한국인 가족의 이야기)에 그대로 갖다 대서,
  **독자가 이미 아는 한국 것**을 한 행씩 세우고 있었다. `novel-data/README.md`의
  「배경지식(bg) 자료를 만드는 규칙」에 「제외하는 것 여섯 가지」와 「한 줄 판정」을 넣어
  고쳤다. **bg를 만들기 전에 그 절을 반드시 읽는다.**
- **다시 만든 첫 결과(54행)는 이번엔 반대로 너무 적었다.** 사용자가 "이렇게 넣을 만한 것이
  없느냐"고 되물어 24행을 더해 78행이 되었다. 판정 기준을 "없으면 장면을 아예 못 읽는가"로
  잡으면 지나치게 깎인다. **"사전을 찾아도, 어휘 시트를 봐도 얻을 수 없는가"가 바른 기준이다.**
- wonder 어휘 **1177행 전체의 논항 표기 정비와 내용 검증이 끝났다.** 여덟 파트 모두
  **시트 반영 대기** — `novel-data/wonder/normalized/paste/`에 붙여넣기용 사본이 있다.
- wonder **배경지식은 1~5장만 정비했다**(11행 → 17행, `wonder/bg-update/`). 6~123장이 남았고
  **bg가 0개인 챕터가 28개**다. 다음에 할 일은 이것이다.
- **계정·세션이 바뀌면 `novel-data/README.md` 맨 앞의 「다른 계정·세션에서 이어받을 때」를
  먼저 읽는다.** 현재 상태와 다음 할 일, 제약이 거기 정리되어 있다.
- `npm test`는 **60/61 통과**. 남은 1개(`앱에 Markdown 파싱 잔재가 남아 있지 않다`)는
  이 작업 이전부터 실패하던 것이다. 아래 "손대지 않은 문제" 참고.

## 로그인 직후 화면과 퀴즈 공개 범위 (2026-08-28 결정)

**로그인하면 소설 선택 화면이 먼저 나온다.** 예전에는 `index.html`의 `#start-screen`에
`hidden`이 없어서, 로그인이 끝나 `#app-container`가 열리는 순간 **`WONDER` 제목과
`assets/wonder.webp` 표지가 그대로 보였다.** 그동안 `openNovelPicker()`는 Apps Script에
권한 목록을 물어보느라 기다리고 있었고, 답이 오고 나서야 목록으로 넘어갔다. 그래서
"wonder 화면이 나왔다가 조금 있으면 소설선택화면으로 넘어간다"는 말이 나왔다.

- `#start-screen`에 `hidden`을 넣었다. 시작 화면은 `chooseNovel()`이 열 때만 나온다.
- `openNovelPicker()`가 **기다리기 전에** `showScreen('novel-screen')`을 먼저 부른다.
  기다리는 동안에는 `#novel-status`에 "소설 목록을 불러오는 중입니다..."를 띄운다.
- 목록 화면을 먼저 띄우면 **기다리는 사이에 학생이 소설을 고를 수 있다.** 그러면
  뒤늦게 도착한 목록이 시작 화면을 덮어 버린다. `novelPickerToken`으로 순번을 매겨,
  `chooseNovel()`이 번호를 올리면 기다리던 `openNovelPicker()`가 조용히 물러난다.
  실제로 이 경합을 브라우저에서 재현해 확인하고 넣은 것이다.

**퀴즈는 선생님이 공개한 챕터만 보인다.** `AllowedUsers` 시트의 소설 열 오른쪽에
`{id}_test` 열(`wonder_test`, `tiger_test`)을 두고 `1-10`처럼 적는다. 퀴즈는 수업에서
함께 푸는 것이라 미리 다 보이면 안 되고, 함께 푼 뒤에 범위를 넓혀 복습하게 한다.
형식과 뜻은 `README.md`의 「`{id}_test` — 퀴즈를 공개한 챕터」에 적어 두었다.

- **빈칸은 '하나도 공개하지 않음'이다.** 해석할 수 없는 값도 마찬가지로 아무것도
  보여 주지 않는다. 넓게 여는 쪽으로 새면 공개 전 퀴즈가 새어 나가기 때문이다.
  `tests/chapter-organization.test.mjs`가 이것을 못 박아 둔다.
- **열이 없으면 동기화가 오류로 멈춘다.** 처음에는 '제한 없음'으로 넘겼는데,
  사용자가 `wonder_test`에 `1-10`을 적었는데도 13장 퀴즈가 보인다고 알려 와
  바꾼 것이다. 열 이름을 잘못 적거나 새 소설에 열을 빠뜨리면 아무 말 없이 퀴즈가
  전부 열려 버렸다. **유출 막는 기능은 조용히 넓게 여는 쪽으로 실패하면 안 된다.**
  모두 열려면 열을 지우지 말고 `all`이라고 적는다.
- **어휘와 배경지식은 가리지 않는다.** 퀴즈만이다.
- 값은 `Code.gs`의 `parseChapterRange()`가 `'1-10,15-15'` 꼴로 다듬어 올리고,
  `js/chapter-organization.js`의 `parseChapterRanges()`가 앱에서 다시 읽는다.
  **두 곳의 규칙이 어긋나면 안 되므로 한쪽을 고치면 다른 쪽도 본다.**
- 거르는 자리는 `js/app.js`의 `applyChapterIndex()` 한 곳이다. 목록을 거른 뒤
  `hiddenQuizChapterCount`를 적어 두어, 공개된 챕터가 없을 때 "자료가 없다"는 오류
  대신 "아직 공개된 퀴즈가 없습니다"를 붉은 글씨 없이 알린다. 오류가 아니다.
- 권한과 공개 범위는 `session` 응답 하나에 함께 실려 오고 `sessionStorage`의
  `novel-app-novel-access` 한 칸에 담긴다. **키 이름이 예전과 다르다**
  (`novel-app-allowed-novels`였다). 시트를 고쳐도 새로고침만으로는 안 바뀐다.
- **로그아웃할 때 이 칸을 지운다**(`clearNovelAccess()`). 교실 컴퓨터 한 대를 여러
  학생이 번갈아 쓰는데, 남겨 두면 다음 학생이 앞 학생의 소설 목록과 퀴즈 공개 범위를
  그대로 물려받는다. 예전 키를 쓰던 때부터 있던 구멍이라 이번에 함께 막았다.

**퀴즈 문제를 앞뒤로 자유롭게 오간다.** 수업에서 함께 푼 뒤 되짚어 보기 위한 것이다.
사용자가 세 번에 걸쳐 방향을 잡아 준 자리이므로 **뜻을 정확히 지킨다.**

- **'이전 문제'는 무조건 바로 앞 문제로 간다.** '내가 푼 것 가운데 이전'이 아니다.
  챕터의 첫 문제에서는 **앞 챕터의 마지막 문제**로 넘어간다. 3장 1번에서 누르면
  2장 마지막 문제가 열리고, 거기서 또 누르면 2장 끝에서 두 번째다.
  `startChapter()`의 `atLastQuestion` 옵션이 그것이다 — 문항 수는 본문을 받아야
  알 수 있어서 부르는 쪽에서 번호를 미리 셀 수 없다.
- 여기서 '앞 챕터'는 **`allChapters`에서 바로 앞**, 곧 공개된 챕터 가운데 앞이다.
  가려 둔 챕터로는 넘어가지 않는다.
- **앞뒤 버튼 둘 다 답을 고르지 않아도 보인다.** 예전에는 '다음 문제'가 답을 고른
  뒤에만 나타났다. 문제만 훑고 지나갈 수 있어야 한다는 것이 사용자 요구다.
  여닫는 일은 `updateQuestionNavigation()` 한 곳에서만 한다.
- **오갈 때는 해설을 되살리지 않는다.** 이미 푼 문제라도 언제 열든 '문제만 나온
  상태'로 시작하고 보기도 다시 고를 수 있다. 한때 되살리게 만들었다가 사용자가
  그러지 말라고 바로잡은 자리다.
- **그래도 점수 기록은 남는다.** 화면만 처음 상태로 돌아갈 뿐, 고른 답은
  `answerSelections`에 남아 있다. 여기서 기록까지 지우면 되돌아갔다는 이유로
  점수가 깎인다. **점수를 `score++`로 더하지 않고** `countCorrectAnswers()`가
  기록에서 매번 다시 세므로, 같은 문제를 두 번 지나가도 두 번 오르지 않고
  다시 풀면 마지막에 고른 답으로 바뀐다.
- 답안은 히스토리 상태의 `answers`에도 적어, 뒤로가기로 돌아와도 점수를 잃지 않는다.
- '다음 문제'는 챕터를 넘지 않는다. 마지막 문제에서는 `결과 보기 ➔`가 되어 기존
  결과 화면과 '다음 챕터를 계속 푸시겠습니까?' 흐름으로 이어진다. 사용자가 요청한
  것은 '풀지 않아도 넘어가기'까지였다.
- 새 Tailwind 클래스를 만들지 않고 `styles.css`에 이미 있는 것만 골라 썼다.
  `npm run build:css`가 네트워크를 타기 때문이다. `justify-end`와 `mr-*`은 없어서
  `justify-between`을 썼다.

> **`Code.gs`를 고쳤으면 저장이 아니라 배포까지 해야 한다.** Apps Script는 코드가
> 두 벌로 돈다. 시트 메뉴의 `허용 명단 동기화`는 편집기에 **저장된** 코드가,
> 앱이 부르는 `session`(`doGet`)은 **배포된** 코드가 처리한다. 붙여넣고 저장만 하면
> 동기화는 성공하는데 `toClientUser()`가 옛것이라 응답에 `quizChapters`가 빠지고,
> 앱은 '제한 없음'으로 보고 퀴즈를 전부 연다. 실제로 이 상태로 한 번 헤맸다.
> 그 상황을 알아채도록 `js/auth.js`의 `getNovelAccess()`가 **응답에 `quizChapters`가
> 없으면 콘솔에 경고**를 남긴다. 퀴즈가 안 막힐 때 이 경고부터 찾는다.

## 원문에서 낱말 누르기 — 읽어 주기와 해석 (2026-09-01)

**원문 화면에서 낱말을 누르면 읽어 주고, 우클릭·길게 누르면 그 문장의 우리말 해석을
문장 아래에 띄운다.** 이북 리더기에서 흔한 기능이라는 사용자 요청이다.

- 한 번 누르기 → 낱말, 두 번 → `문장`·`문단` 차림표, 우클릭/길게 누르기 → 해석.
  한 번과 두 번은 260ms로 가른다. 두 번 누르면 예약해 둔 낱말 읽기를 취소한다.
- **해석은 Apps Script의 `LanguageApp.translate()`가 만든다.** voca-main 앱이 쓰는
  방식을 그대로 맞췄다. **API 키가 필요 없고 요금도 들지 않는다.** `Code.gs`에
  `translate` 액션을 더했고 `doGet`이 먼저 로그인을 확인하므로 승인된 학생만 부른다.
  **고쳤으면 배포까지 해야 한다** — 저장만 하면 '지원하지 않는 요청입니다'가 난다.
- 옮긴 문장은 `library-cache.js`의 IndexedDB에 `translation:` 키로 담는다. 원문은
  바뀌지 않으므로 한 번 옮기면 계속 쓴다. 같은 문장을 연달아 눌러도 요청은 하나다.
- **글자 고르기(selection)에 기대지 않는다.** 앱이 `selectstart`를 막아 두어 두 번
  눌러도 낱말이 선택되지 않는다. 대신 `caretRangeFromPoint()`로 눌린 자리를 좌표로
  찾는다. 문단을 `innerText`로 채워 글자 마디가 하나뿐이라 마디 안의 자리가 곧
  문단 안의 자리다. **문단을 여러 마디로 쪼개도록 바꾸면 이 계산이 깨진다.**
- 낱말·문장 잘라내기는 `js/text-tools.js`의 순수 함수이고 테스트가 규칙을 못박는다.
  `Mr.`에서 끊지 않고, `“Get up!” she yelled.`처럼 **닫는 따옴표 뒤가 소문자면 아직
  한 문장으로 본다**(끊으면 '누가 말했는지'가 떨어져 나간다).
- **해석 카드에는 `flex`를 걸지 않는다.** 차림표(버튼 둘)는 가로로 놓아야 하지만
  카드는 위에서 아래로 쌓인다. 한 함수에서 둘 다 `flex`로 열었다가 원문·해석·닫기가
  세 칸으로 찌그러졌다. `placeNearRange(..., { row })`로 가른다.
- 팝업은 **문장의 마지막 줄 아래**에 붙이고, 아래 자리가 모자라면 위로 올린다.
  본문에 끼워 넣지 않는 이유는 **두 단 쪽 넘김에서 글이 밀려 쪽이 다시 짜이기**
  때문이다. 쪽을 넘기거나 챕터를 옮기면 팝업을 닫는다.
- 아이폰·아이패드에서는 `speakEnglish()`가 일부러 아무 것도 하지 않는다. 눌렀는데
  조용하면 고장으로 보이므로 **그때만** 안내 문구를 띄운다.

## "OOO 원문 firebase로 올리고 싶어"라고 하면

사용자가 이렇게 말하면 **`README.md`의 「새 소설의 원문을 올리는 절차」를 그대로
안내한다.** 사용자가 직접 손으로 하는 작업이므로 **번호 매긴 절차를 먼저 주고 설명은
뒤에 붙인다.** 명령은 소설 id를 실제 값으로 바꿔서, 한 줄씩 복사해 붙일 수 있게 낸다.

역할이 나뉜다.

- **1단계(epub → 챕터별 텍스트 추출)는 내가 한다.** epub은 판본마다 구조가 달라
  정해진 명령이 없다. `novel-data/tools/extract.py`(tiger)와 `extract_wonder.py`
  (wonder, toc.ncx 기준)가 본보기다. 결과는 `novel-data/{id}/text/ch01.txt...` 꼴이고
  **한 문단이 한 줄**이어야 한다.
- **2·3단계(JSON 만들기, 올리기)는 사용자가 한다.** 비밀키를 내가 만지지 않기 위해서다.
  `build_text.py`와 `upload_text.py`가 환경 변수로만 비밀키를 받는다.

**빠뜨리기 쉬운 것을 먼저 짚어 준다.**

- `novels` 시트에 `active=yes`로 있는가, `AllowedUsers`에 그 소설 열이 있는가,
  **`허용 명단 동기화`를 돌렸는가.** 마지막 것을 빼먹으면 보안 규칙이 원문을 막아
  `permission_denied`가 난다. 증상만 보면 원인을 짐작하기 어렵다.
- **보안 규칙은 고칠 필요가 없다.** `novel/text/$novelId` 규칙이 소설 이름을 가리지
  않아 새 소설도 그대로 보호된다. 규칙을 또 붙여넣으라고 하지 않는다.
- 사용자 PC에서는 **파이썬의 HTTPS가 막힌다**(백신·망의 인증서 검사).
  `upload_text.py`가 `curl`을 먼저 쓰도록 되어 있으니 그대로 두면 된다.

**절차를 마치면 `README.md`와 이 문서를 손봐야 하는지 본다.** 새 소설의 추출 방식이
기존 둘과 달랐다면 그 도구와 까닭을 남긴다.

## 원문 읽기 (2026-09-01)

**시작 화면에 `Reading Text` 버튼을 더했다.** 어휘(빨강)·퀴즈(파랑) 아래에 **두 버튼을 합친
너비**로 두고 **녹색** 테마를 줬다. 들어가면 챕터 목록이 나오고, 챕터를 고르면 원문이
문단으로 보인다. **본문 맨 끝에 `다음 챕터로 이동 ➔` 버튼**이 있다.

- 버튼 폭은 바깥을 `inline-flex`로 감싸 얻었다. 그러면 그 칸의 폭이 위 두 버튼 줄에
  맞춰지고, `Text` 버튼의 `w-full`이 정확히 '둘을 합친 너비'가 된다. 실제로 재 보니
  Word 왼쪽 끝과 Quiz 오른쪽 끝에 딱 맞는다.
- **원문은 시트를 거치지 않는다.** 한 번 만들면 고칠 일이 없어 시트의 이점이 없고,
  산문을 셀에 붙여넣으면 큰따옴표·줄바꿈으로 깨진다(CLAUDE.md가 이미 겪은 일).
- **`novel/content`가 아니라 `novel/text`에 올린다.** 시트 동기화가 `novel/content`와
  `novel/content/{id}`를 **PUT으로 통째로 갈아 끼우기** 때문에, 안에 두면 다음 동기화
  때 조용히 사라진다. `Code.gs`를 안 고치고 피할 수 있는 유일한 자리다.
- **보안 규칙은 소설별로 좁게 잡았다** — `accessByUid/$uid/novels/$novelId`가 true인
  학생만 읽는다. `novel/content`는 승인만 되면 다 열리지만 원서 전문은 이야기가 다르다.
  별도 서브트리라 기존 규칙을 건드리지 않고 쓸 수 있었다. **잠기는 쪽으로 실패한다** —
  `허용 명단 동기화`를 안 돌렸으면 원문이 안 보인다. 그게 맞는 방향이다.
- **버전은 내용에서 뽑는다**(`build_text.py`가 sha256 앞 12자리). 원문이 그대로면
  번호도 그대로라 학생 기기가 두 번 다시 내려받지 않는다. 그래서 어휘·퀴즈처럼
  버전을 구독하지 않고, 목록을 받을 때 한 번만 맞춰 본다.
- **미리받기(`prepareLibraryCache`)에는 넣지 않았다.** 두 소설 원문이 1.1MB라 모바일
  데이터로 통째로 받게 하고 싶지 않다. 챕터 하나는 3.5~17KB라 고를 때 받으면 된다.
- **글자 크기는 1px씩** `+` `−`로 조절하고(12~32px, 기본 16px) 값은 `localStorage`에
  기기별로 남는다. `#text-body`의 `--text-body-size`를 바꾸는 방식이라 문단만 커지고,
  `다음 챕터로 이동` 버튼은 `text-base`를 줘서 그대로 둔다.
- **넓은 화면(1024px 이상)에서는 이북 리더기처럼 두 단으로 보고 쪽을 넘긴다**
  (2026-09-01). 한 단을 화면 높이에 맞추고(`column-fill: auto`) 넘치는 만큼 오른쪽으로
  이어 붙여서, 읽는 순서가 **왼쪽 칸 → 오른쪽 칸**이 된다. 단을 그냥 둘로 나누면 왼쪽
  칸을 챕터 끝까지 읽고 다시 맨 위로 올라가야 해서 못 쓴다.
  - **가로 스크롤바를 두지 않는다.** CSS는 `overflow: hidden`이고, 옮기는 일은 앱이
    `scrollLeft`로만 한다(`overflow:hidden`이어도 코드에서 옮기는 것은 된다).
    처음에 스크롤바로 만들었다가 사용자가 "책장 넘기듯"이라고 바로잡은 자리다.
  - **한 쪽은 `clientWidth + column-gap`이다.** 이 폭의 배수로만 옮겨야 단 경계가
    맞는다. 화면 아래 `#text-pager`가 `2 / 5`처럼 쪽 번호를 보여 주고 양 끝에서
    버튼이 꺼진다.
  - 넘기는 방법은 넷이다 — 쪽 넘김 버튼, `← →`·`PageUp/PageDown`, 휠(한 번에 한 쪽,
    관성 스크롤이 여러 번 들어와도 400ms 잠금), 손가락 쓸기(40px 이상).
    **브라우저는 세로 휠을 가로 이동으로 바꿔 주지 않는다**(실제로 확인).
  - **넘김 효과는 없다.** `scrollLeft`를 한 번에 옮겨 글자만 바뀐다. 한때 책장이
    넘어가는 3D 효과를 넣었다가(`playPageFlip`) **사용자가 "가장 심플하게, 어떤 화면
    전환 효과도 없이"라고 하여 통째로 걷어냈다.** 다시 넣자는 이야기가 나오면 이
    결정을 먼저 확인한다. 부드러운 스크롤(`behavior: 'smooth'`)도 쓰지 않는다.
  - 한 단일 때는 모든 처리가 그냥 지나가고(`textIsPaged()`) 쪽 넘김 막대도 숨는다.
    글자 크기를 바꾸거나 창 크기가 바뀌면 단이 다시 짜이므로 첫 쪽으로 되돌리고 다시 센다.
- **본문은 양쪽 맞춤이다**(2026-09-01). 맞춤만 하면 좁은 단에서 단어 사이가 들쭉날쭉해져
  하이픈(`hyphens: auto`)도 함께 켰고, 영어 규칙을 따르도록 `#text-body`에 `lang="en"`을
  달았다. 페이지 전체는 `lang="ko"`라 이 칸에만 따로 준 것이다.
- `library-cache.js`의 `normalizeKind`에 `'text'`를 넣었다. **안 넣으면 quiz로 접혀
  퀴즈 캐시를 덮어쓴다.**
- `tests/lazy-chapters.test.mjs`의 '정규화 호출 수'를 1→2로 고쳤다. 원문도 Firebase에서
  막 받은 자리에서 한 번만 정규화하므로 규칙의 뜻은 그대로다.

**wonder는 원문 123챕터인데 어휘가 121챕터뿐이다** — 60장(Fortune Favors the Bold)과
85장(Hearing Brightly)에 어휘가 없다. 퀴즈는 123챕터 다 있고 배경지식도 그 둘은 있다.
로컬 CSV 기준이라 시트에는 있을 수 있다(bg에 그 두 챕터가 있는데 `Code.gs`는 word에
없는 챕터를 bg에서 거부하므로, 동기화가 성공하고 있다면 시트에는 있다는 뜻이다).
**원문 목록은 파일에서 만들므로 이 문제와 무관하게 123챕터가 다 나온다.**

## 어휘·배경지식 검색 (2026-09-01)

**어휘 챕터 선택 화면 오른쪽 위에 돋보기 버튼이 있다.** 누르면 검색창이 뜨고, 찾은
뒤에는 **걸린 카드가 있는 챕터만 목록에 남고, 챕터에 들어가면 걸린 카드만 보인다.**

- **찾는 곳은 세 가지뿐이다** — 어휘 카드의 `word`(표제어)와 `sentence`(예문),
  배경지식 카드의 `eng`(영어 문구). **파생어 칩(동의어·반의어)과 연어, 어휘의
  한국어 뜻, 배경지식의 우리말 뜻·설명(`kor`·`remark`)은 일부러 뺐다.** 학생이 찾는
  것은 소설에 실제로 나온 표현이라, 태그나 설명까지 걸리면 엉뚱한 챕터가 함께 나온다.
  (처음에는 배경지식 카드 전체를 찾았는데 사용자가 `eng`만으로 좁히라고 바로잡았다.)
- **대괄호 표시를 지우고 맞춰 본다.** 시트의 `sentence`는 강조할 어휘를 `[ ]`로
  감싸므로, 그대로 두면 `all gotten pretty good at`처럼 대괄호를 걸치는 문구가
  걸리지 않는다. `[RED: ]`·`[SENTENCE]`도 같이 지운다. 굽은 따옴표는 곧은 것으로
  맞추고, 대소문자는 무시한다. 부분 문자열 포함이면 걸린다.
- **검색은 퀴즈에는 없다.** 퀴즈 챕터 화면은 그대로 두었다.
- **검색하려면 어휘 본문을 다 받아야 한다.** `js/auth.js`의 `loadAllChapters()`가
  기기에 받아 둔 것을 먼저 쓰고, 하나라도 빠져 있을 때만 `chapters` 노드를 한 번에
  받는다. 챕터마다 따로 부르면 백 번이 넘는 왕복이 된다. 시작 화면에서 도는
  `prepareLibraryCache()`가 대개 이미 다 받아 두므로 실제로는 기다림이 없다.
- **일치하는 것이 없으면 빈 목록을 보여 주지 않는다.** 창을 열어 둔 채 "일치하는
  카드가 없습니다"라고 알려, 검색어를 고쳐 바로 다시 찾게 한다.
- **카드 번호는 원래 자리를 지킨다.** 걸린 카드만 보여 줄 때도 `1, 2, 3`으로 다시
  매기지 않는다. 몇 번째 어휘인지 알아볼 수 있어야 하기 때문이다.
- **검색 중에는 어휘 화면의 ← →도 걸린 챕터끼리만 오간다**(`findAdjacentWordChapter()`).
  목록에서 뺀 챕터로 넘어가면 카드가 하나도 없는 화면이 나온다.
- 검색어 칩은 **두 화면 모두**(챕터 목록, 어휘 화면)에 있어 어디서든 ✕로 풀 수 있다.
  어휘 화면에서 풀면 보던 챕터는 그대로 두고 가려 둔 카드만 되살린다.
- 소설을 바꾸면 검색은 자동으로 풀린다. 결과가 그 소설의 챕터 자리를 가리키기 때문이다.
- **목록을 거를 때 index를 흔들지 않는다.** `renderGroupedChapterList()`는 걸러진
  배열을 받지 않고, 묶은 뒤에 `includeChapter`로 덜어 낸다. `onSelect`가 넘겨받는
  번호는 언제나 `allWordChapters`의 자리 그대로여야 한다.
- **검색창 안에서는 글자를 고를 수 있어야 한다.** 앱은 `selectstart`·`contextmenu`를
  document에서 막고 있으므로, `#search-modal`에서 그 이벤트의 전파를 멈춘다.
  document의 두 줄은 `tests/dom-contract.test.mjs`가 글자 그대로 검사하므로 건드리지 않는다.
- 새 Tailwind 클래스는 만들지 않고 `styles.css`에 이미 있는 것만 골라 썼다
  (`npm run build:css`가 네트워크를 타기 때문). `hover:text-*`는 아예 없어서
  칩의 ✕에는 `hover:bg-red-100`을 썼고, 검색창 포커스 색은 파란 계열을 그대로 썼다.

## 권한을 Firebase에서 바로 읽는다 (2026-08-28)

**승인된 학생의 소설별 권한과 퀴즈 공개 범위는 `novel/accessByUid/{uid}`에서 바로
읽는다.** 예전에는 소설 목록을 띄우기 전에 Apps Script `session`을 기다렸는데, 그
한 번의 요청이 리다이렉트·콜드 스타트에 더해 **외부 HTTP 호출을 순서대로 셋**
(토큰 검증 → `allowedUsers` 전체 GET → 기록 PATCH) 하느라 몇 초씩 걸렸다. 사용자가
"Firebase에서 가져오는 건데 왜 로딩이 걸리냐"고 물어 드러난 것이다. 목록 자체는
처음부터 빨랐고, 느린 것은 권한 확인이었다.

- 필요한 값은 `buildAccessByUid()`가 이미 그 노드에 올려 두고 있었다. 막고 있던 것은
  보안 규칙뿐이라 **규칙에 자기 uid만 읽는 한 줄**을 더해 열었다.
  규칙은 아래로만 내려가므로 `accessByUid` 전체를 훑는 것은 여전히 막힌다.
- **규칙은 저장소 파일이 아니라 Firebase 서버에 있다.** `firebase.rules.json`은 기록용
  사본이고 Firebase가 자동으로 읽지 않는다. 콘솔의 규칙 화면에 붙여 넣어야 적용된다.
  사용자가 이 부분을 헷갈려 했으므로 안내할 때 분명히 말한다.
- 되돌아갈 길을 남겼다. `readNovelAccessFromDatabase()`가 **규칙이 아직 좁거나
  기록에 `quizChapters`가 없으면 null을 돌려주고**, 부르는 쪽이 예전처럼 Apps Script로
  간다. 그래서 규칙을 바꾸기 전에 앱을 올려도 동작은 그대로다 — **앱 먼저, 규칙 나중도
  안전하다.**
- **직접 읽은 결과는 `sessionStorage`에 담지 않는다.** 읽기가 워낙 빨라 담을 이유가
  없고, 담지 않으면 시트를 다시 동기화했을 때 **탭을 새로 열지 않고 새로고침만 해도**
  반영된다. Apps Script로 되돌아간 경우에만 예전처럼 담는다.
- 아직 승인되지 않은 사용자를 확인하는 `callScript('session')`은 그대로 남는다.
  그 사람에게는 `accessByUid` 기록 자체가 없다.
- 이 길이 살아 있는지는 `tests/firebase-integration.test.mjs`가 지킨다.

## 다중 소설 구조에서 알아둘 점

- 소설 목록은 `novels` 시트가 원본이고, 동기화하면 `novel/content/novels`에 올라간다.
  앱은 **이 노드를 읽어 승인 여부를 판단한다.** 노드가 없으면 로그인 자체가 막히므로,
  `Code.gs`를 새로 배포한 뒤에는 반드시 `학습 자료 동기화 (전체)`를 한 번 돌린다.
- `novels` 시트의 `id`는 세 곳에서 같은 이름으로 쓰인다. **시트 이름 접두어**(`tiger_word`),
  **`AllowedUsers`의 열 이름**, **Firebase 경로**(`novel/content/tiger`).
- `AllowedUsers`의 소설별 열은 **빈칸이면 오류**다. 반드시 `yes` 또는 `no`.
- 소설별 권한 목록은 브라우저 `sessionStorage`에 담아 둔다. 시트를 고치고 다시 동기화해도
  **새로고침만으로는 바뀌지 않는다.** 탭을 새로 열거나 `sessionStorage.clear()`를 실행한다.

## 화면 폭과 TTS (2026-08-26 결정)

**넓은 화면에서는 목차와 어휘 내용만 여러 열로 편다.** `#chapter-screen`,
`#word-chapter-screen`, `#word-screen`만 넓힌다. **1024px 이상은 72rem에 두 열**,
**1536px 이상은 100rem에 세 열**이다. 한 칸 폭이 두 열일 때(약 540px)와 비슷하게 남도록
열을 늘릴 때 카드 폭도 함께 키운다. 네 열은 만들지 않는다.

- 목차는 **파트 헤더를 가로 전체로 두고 그 안의 챕터 버튼만** 두 열로 나눈다. 파트 묶음
  자체를 두 열로 하면 파트가 없는 소설(tiger)은 한 묶음뿐이라 오른쪽이 통째로 빈다.
- 규칙은 **Tailwind 클래스가 아니라 `index.html`의 `<style>` 블록**에 id 기준으로 적었다.
  클래스로 쓰면 `npm run build:css`를 다시 돌려야 하는데 그 빌드는 네트워크를 탄다.
  `tests/styles-build.test.mjs`도 `class="..."`에 쓴 클래스만 검사하므로 걸리지 않는다.
- CSS가 지금 어느 화면인지 알아야 해서 `showScreen()`이 `#app-container`에 `data-screen`을
  적고, 챕터 묶음에는 `data-chapter-panel`을 단다. 접힌 묶음까지 grid가 되면 다시 펼쳐지므로
  선택자에 `:not(.hidden)`이 반드시 들어간다.
- `space-y-*`는 첫 항목을 뺀 전부에 `margin-top`을 단다. 여러 열에서는 첫 줄 왼쪽 칸만 위로
  밀리므로 미디어 쿼리 안에서 지우고 `gap`으로 대신한다.
- **같은 줄에 놓인 카드는 그중 가장 긴 것에 높이를 맞춘다**(`align-items: stretch`). 각자
  높이를 쓰면 아래 선이 들쭉날쭉해 보인다는 사용자 지적에 따른 것이다.
- **배경지식은 어휘가 끝난 다음 줄에서 시작한다.** 어휘가 홀수 개여서 마지막 줄 오른쪽이
  비더라도 그 자리에 끼워 넣지 않는다. `Code.gs`가 어휘를 모두 담은 뒤 배경지식을 잇대므로
  (`Code.gs`의 `wordRows` → `backgroundRows` 순서), 첫 배경지식 카드에만
  `data-background-start`를 달고 그 카드를 첫 칸으로 민다. 순서가 섞이도록 바뀌면 이 규칙도
  다시 봐야 한다.

**퀴즈를 푸는 화면은 답을 고른 뒤에만 두 열이 된다.** 문제만 있을 때는 넓은
화면에서도 672px 한 열로 둔다. 답을 고르면 카드가 72rem으로 넓어지면서 네 칸이 된다.

| 줄 | 왼쪽 | 오른쪽 |
|---|---|---|
| 1 | `#quiz-header` (두 열에 걸침) ||
| 2 | `#quiz-question` | 비움 |
| 3 | `#options-container` | `#quiz-answer-pane` (해설 + ‘다음 문제’) |

**해설은 첫 보기와 같은 높이에서 시작한다.** 보기와 해설을 같은 줄(3번 줄)에 놓아 얻은
것이다. 문제와 해설을 한 칸씩 묶어 두 칸으로만 나누면 해설이 문제 윗줄에 맞춰져
첫 보기보다 위에서 시작한다. 사용자가 바로잡아 준 지점이다. 문제를 가로 전체로 벌리는
방법도 있지만, 그러면 답을 고를 때마다 문제 글줄이 다시 접혀 읽던 자리를 잃는다.

**문제를 좌우로 둘씩 보이게 하지는 않고**, 퀴즈는 1536px 이상에서도 세 열로 가지 않는다.

- 답 여부는 `loadQuestion()`과 `selectOption()`이 `#app-container`에 `data-quiz-answered`로
  적는다. 해설은 `#quiz-screen` 안에 있지만 카드 폭은 `#app-container`가 정하므로,
  `:has()` 없이 둘 다 다루려면 표시가 바깥쪽에 있어야 한다.
- 한 열일 때는 `#quiz-answer-pane`만 묶음이고 나머지는 예전 그대로 차례대로 쌓인다.
- 한 열일 때 보기와 해설을 띄우던 `mt-6`은 두 열에서 해설을 첫 보기보다 아래로
  밀어내므로 미디어 쿼리 안에서 지운다.

**아이폰·아이패드에서는 TTS를 하지 않는다.** iOS의 Web Speech 음성 품질이 너무 낮아 없는
편이 낫다는 사용자 판단이다. `js/tts.js`의 `isIosDevice()`로 판별해 `speakEnglish()` 초입에서
막는다. 맥은 Alex·Samantha가 쓸 만하므로 막지 않으며, iPadOS 13+가 데스크톱 Safari인 척하는
것은 `maxTouchPoints`로 가려낸다(맥은 0). 화면에 발음 표시가 원래 없어서 안내문도 두지 않았다.

## 소설 원본과 자료 작업

수업용 사용은 **교육청 승인을 받았다.** 저작권 때문에 막을 것은 없다.

원본 epub, 추출한 본문, 생성·검증 스크립트는 `novel-data/`에 있고
**`.gitignore`로 제외되어 GitHub에는 올라가지 않는다.** 앱은 이 파일들을 런타임에 전혀
읽지 않는다 — 코드 어디에도 `epub`이나 `.pdf` 참조가 없다. GitHub Pages가 저장소 전체를
서빙하므로 올릴 이유도 없다.

**자료를 만들기 전에 `novel-data/README.md`를 읽는다.** 특히 두 절이 기준이다.

- **「어휘(word) 자료를 만드는 규칙」** — `meaning`의 ①은 반드시 그 행의 `sentence`에서
  쓰인 뜻이고 품사까지 같아야 한다. ② 이하는 고1~2 빈도순 두세 개. 사전 순서를 그대로
  옮기면 대개 ①이 어긋난다.
- **「작업 방식」** — 20행씩 끊어서 만들고 그때그때 본문과 대조한다. **형식 검사를
  통과했다고 뜻이 맞는 것이 아니다.** 스크립트는 표기와 구조만 본다.

**작업을 마칠 때마다 그 문서의 진행 표와 작업 기록을 갱신한다.** 사용자가 시키지 않아도
한다. 세션이 바뀌어도 같은 품질이 나오게 하는 유일한 장치다. 사용자가 방향을 바로잡아
주면 규칙 자체를 고치고 **왜 바뀌었는지 이유까지 남긴다.**

> `.gitignore`에서 `novel-data/`를 빼면 원서 전문이 그대로 올라간다. 지우지 않는다.

## 아직 하지 않은 것

- **소설별 접근 제어가 화면 단에서만 동작한다.** `firebase.rules.json`은 여전히
  `novel/content` 전체를 승인된 사용자에게 열어 준다. tiger만 허가된 학생도 마음먹으면
  wonder 자료를 받아볼 수 있다. 사용자가 "일단 화면에서만 숨김"을 택한 결과다.
  서버에서 막으려면 규칙을 `novel/content/$novelId` 단위로 쪼개고
  `accessByUid/$uid/novels/$novelId`를 검사하면 된다. **Apps Script는 이미 그 값을 올리고
  있으므로 규칙만 고치면 된다.**
- `manifest.json`의 이름이 아직 `Wonder Reading Journey`다. 설치된 앱 이름이 바뀌는 일이라
  사용자 확인을 받고 고친다.

## 손대지 않은 문제

`tests/firebase-integration.test.mjs`의 `앱에 Markdown 파싱 잔재가 남아 있지 않다`가 실패한다.
`js/library-loader.js`, `js/content-parser.js`, `scripts/generate-manifest.mjs`가 없어야 한다고
검사하는데 파일이 남아 있다. 그런데 `tests/library-loader.test.mjs`와
`tests/content-parser.test.mjs`는 **그 파일들을 import 한다.** 지우면 다른 테스트가 깨진다.
마이그레이션이 중간에 멈춘 상태이며, 이번 작업과 무관하므로 건드리지 않았다.

## CSS

`npm run build:css`가 **정상 동작한다**(네트워크 필요). `index.html`이나 `js/*.js`에서
클래스를 추가·삭제했다면 반드시 다시 빌드하고 `styles.css`도 함께 커밋한다.
빠뜨리면 `tests/styles-build.test.mjs`가 잡아 준다.

## 사용자 선호

- 대화는 한국어로 한다.
- **`assets/` 폴더에 임시 파일을 만들지 않는다.** 과거에 레이아웃 확인용 더미 이미지를
  만들었다가 지우면서 사용자가 넣어 둔 실제 파일을 삭제한 적이 있다. 검증이 필요하면
  스크래치패드에 사본을 만들어 쓴다.
- 코드를 고치기 전에 검토만 요청하는 경우가 있다. "검토만 해줘"라고 하면 코드를 건드리지 않는다.
